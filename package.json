{
  "name": "query-demo",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^1",
    "@sap/cds": "^8",
    "cds-swagger-ui-express": "^0.10.0",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.10",
    "@cap-js/sqlite": "^1",
    "@cap-js/cds-typer": ">=0.35",
    "@types/node": "^22.0.0",
    "tsx": "^4",
    "typescript": "^5"
  },
  "scripts": {
    "start": "cds-serve",
    "clean-node-force": "npm run clean-node && rm -rf package-lock.json",
    "clean-node": "rm -rf node_modules"
  },
  "cds": {
    "cdsc": {
      "severities": {
        "odata-spec-violation-no-key": "Warning"
      }
    },    
    "fiori": {
      "preview": true
    },
    "server": {
      "index": true
    },
    "[hybrid]": {
      "requires": {
        "db": {
          "kind": "hana-cloud"
        }
      }
    }
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  }
}
